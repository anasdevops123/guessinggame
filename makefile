
README.md:
	echo "The title of the project is GUESSING_GAME" > README.md
	echo 

	echo "Date when make was run is $(shell date)" >> README.md
	echo

	echo "Number of lines of code contained is 50" >> README.md
	echo

	echo "1. This project reads the number of files in the current directory and ask user to make a guess. 2. If the guess is correct program will exit
	with a message else will continue till the user does not guess it correctly." >> README.md

	echo 
	echo "Script can be run as bash guessinggame.sh" >> README.md



